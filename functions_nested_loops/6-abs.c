#include "main.h"
#include <stdio.h>

/**
* _abs - checks value of number
* @num: parameter to hold value
* Return: num or -num
*/
int _abs(int num)
{
if (num >= 0)
{
return (num);
}
else
{
return (-num);
}
}
